[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm install -g pnpm@9",
  "cd artifacts/luneth-bot && pnpm install --no-frozen-lockfile"
]

[phases.build]
cmds = ["cd artifacts/luneth-bot && pnpm run build"]

[start]
cmd = "npx serve artifacts/luneth-bot/dist/public -l $PORT"
