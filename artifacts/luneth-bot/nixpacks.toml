[phases.setup]
nixPkgs = ["nodejs_22", "nodePackages_latest.pnpm"]

[phases.install]
cmds = ["pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "npx serve dist/public -l $PORT"
